import { FechaConexionPipe } from './fecha-conexion.pipe';

describe('FechaConexionPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaConexionPipe();
    expect(pipe).toBeTruthy();
  });
});
