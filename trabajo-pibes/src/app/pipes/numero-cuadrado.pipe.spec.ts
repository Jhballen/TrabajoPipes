import { NumeroCuadradoPipe } from './numero-cuadrado.pipe';

describe('NumeroCuadradoPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroCuadradoPipe();
    expect(pipe).toBeTruthy();
  });
});
