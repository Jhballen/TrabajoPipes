import { MayusAlterPipe } from './mayus-alter.pipe';

describe('MayusAlterPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusAlterPipe();
    expect(pipe).toBeTruthy();
  });
});
